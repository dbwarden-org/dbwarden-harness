# Atlas Adoption Fixture

This fixture represents an Atlas-managed schema handed to DBWarden. The
handoff preserves the live schema as the baseline, then uses DBWarden's public
model and migration commands for subsequent changes.
